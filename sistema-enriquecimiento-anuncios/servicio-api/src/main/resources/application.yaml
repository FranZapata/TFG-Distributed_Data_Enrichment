server:
  port: 8084

spring:
  application:
    name: servicio-api

  datasource:
    url: jdbc:postgresql://localhost:5432/tfgdb
    username: tfguser
    password: tfgpassword

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
