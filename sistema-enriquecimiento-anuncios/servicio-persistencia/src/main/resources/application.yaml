server:
  port: 8083

spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: persistencia-anuncios-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer

      properties:
        spring.json.use.type.headers: false
        spring.json.value.default.type: com.tfg.anuncios.contratos.evento.AnuncioProcesadoEvent
        spring.json.trusted.packages: "*"

  datasource:
    url: jdbc:postgresql://localhost:5432/tfgdb
    username: tfguser
    password: tfgpassword

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: servicio-persistencia